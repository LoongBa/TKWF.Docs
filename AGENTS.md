# Sisyphus 工作规则（TKWF.Docs）

> 本文件记录 Sisyphus 在本仓库（TKWF.Docs，public 文档站项目）中的持续维护职责与规则。

---

## 一、TKWF.Docs 文档站设计方案维护规则

### 1.1 文档位置

| 项 | 值 |
|:---|:---|
| **方案主文档** | `../_TKWF/docs/TKWF.Docs_设计文档/TKW.Framework-文档站设计方案.md`（即 `F:\LoongBa_Git\_TKWF\docs\TKWF.Docs_设计文档\`，**文件名固定不带版本号**，版本信息在文档头部元数据中维护） |
| **设计方案变更记录** | `../_TKWF/docs/TKWF.Docs_设计文档/变更记录/V1.x.x-变更记录.md`（设计文档自身迭代时记录） |
| **内容变更记录** | `docs/变更记录/V4.9.x-内容对齐记录.md`（文档站内容对齐 TKWF 新版本时记录） |
| **版本命名** | 主文档文件名固定为 `TKW.Framework-文档站设计方案.md`（不带版本号）；版本号仅在文档头部元数据 `> 版本：V{大}.{小}.{修订}` 中维护 |
| **变更记录命名** | `V{大}.{小}.{修订}-变更记录.md` |
| **对齐 TKWF 版本** | 主文档与变更记录**开头**均须标注对齐的 TKWF 版本号 + 日期（如 `对齐 TKWF：V4.9.12 · 2026-08-04`），以 `_TKWF/docs/CHANGELOG.md` 为准 |
| **文档站 tag** | 与 TKWF 框架版本对齐（如 `v4.9.12`），`pwsh scripts/tag.ps1` 自动取 `_TKWF` 最新 `v*` tag 创建 |

> **为什么存在 `../_TKWF/` 而不是本仓库？**
> - TKWF.Docs 是 **public 仓库**（GitHub Pages 站点），提交即公开
> - 方案文档属于内部规划，必须保存在私有仓库 `_TKWF` 的 `docs/` 下，既被 git 跟踪又不被公开
> - `_TKWF` 仓库的 `.gitignore` 已忽略 `.sisyphus/`，但 `docs/TKWF.Docs_设计文档/` 是被跟踪的

### 1.2 版本管理规则

> **设计文档的版本与内容对齐是两个独立维度**：
> - **设计文档版本**（V1.0 → V1.0.1 → V1.1…）：设计文档自身的迭代版本，反映文档结构变化
> - **内容对齐版本**（TKWF V4.9.12…）：设计文档描述和引用的框架版本，反映内容准确性
>
> 设计文档版本与 TKWF 框架版本**无对应关系**——一个设计文档版本可能对应多个 TKWF 版本（如 V1.0.1 对齐 TKWF V4.9.12），一个 TKWF 版本也可能有多个设计文档迭代。

- **大版本（V1 / V2 / V3…）**：结构性重构（导航架构、引用机制、设计原则变更）时升级。发布新大版本时，将旧大版本方案文档备份到 `../_TKWF/docs/TKWF.Docs_设计文档/_Archive/V{n}/` 子目录。
- **子版本（V1.0 / V1.1 / V1.2…）**：新增章节、内容调整、站点功能增强时升级。**主文档文件名固定不变**——内容保持最新，旧版本通过 git 历史与变更记录追溯，不保留多份版本文件。
- **修订（V1.0.1…）**：错别字、格式修正、术语同步等小改动，直接修改当前版本文件，更新头部元数据中的版本号。

### 1.3 每次子版本迭代必做

每次子版本迭代完成后，**必须**做两件事：

1. **编写变更记录**：在 `变更记录/` 目录下新增一份变更记录文档：

```
V1.x.x-变更记录.md
```

2. **更新主设计文档**：将 `TKW.Framework-文档站设计方案.md` 头部元数据中的版本号更新为最新，**内容保持最新**（旧内容仅存于 git 历史，不在文档中堆叠）。

**变更记录模板**：

```markdown
# TKWF.Docs 变更记录 — V1.x.x

> 对齐 TKWF：V4.9.x · 日期：YYYY-MM-DD · 状态：已实施/已审核/已发布
> 前置版本：V1.x.y（日期，迭代来源）

## 一、本次变更

| 类型 | 说明 |
|:-----|:-----|
| 新增 | 新增了哪些文档/章节/页面 |
| 修改 | 修改了哪些现有内容 |
| 删除 | 删除了哪些内容 |

## 二、涉及文件

| 文件路径 | 变更类型 |
|:---------|:---------|
| `docs/TKWF.Docs_设计文档/TKW.Framework-文档站设计方案-V1.x.x.md` | 新增/修改 |
| … | … |

## 三、变更背景（如适用）

用户补充的需求 / 评审意见 / 事实核验结果……

## 四、审核记录

| 审核人/工具 | 结果 | 日期 | 意见摘要 |
|:-----------|:-----|:-----|:---------|
| Momus | ✅ 通过 | YYYY-MM-DD | … |

## 五、遗留事项

- …
```

### 1.4 维护检查清单（每次会话开始/结束时检查）

- [ ] 方案主文档当前版本号是否与最新版本一致
- [ ] 是否缺少最近一次子版本迭代的变更记录
- [ ] 大版本切换时是否完成 `_Archive/V{n}/` 备份
- [ ] 术语是否与 TKWF 主仓库 CHANGELOG 同步（V4.9.12 当前）
- [ ] 本仓库引用的版本号/功能点是否以 `_TKWF` 源码与 `docs/CHANGELOG.md` 为准核对过（依赖方向不可颠倒）

---

## 二、TKWF.Docs 站点维护规则

> **单一事实来源（Source of Truth）原则**：所有内容以 `_TKWF` 仓库为准，依赖关系不可颠倒。

### 2.0 依赖方向（强制）

```
_TKWF（源码 + docs/ 源文档）  ←——— 唯一事实来源 ———  TKWF.Docs（展示/衍生层）
```

- **`_TKWF` 是权威来源**：框架源码、`docs/` 下 D/G/T 系列文档、CHANGELOG 版本历史，均为**事实**，任何情况下不得以 TKWF.Docs 内容反向推断事实
- **TKWF.Docs 是衍生层**：本仓库所有文章、README、方案文档，都是对 `_TKWF` 事实的**转述/组织/编排**，不得自创事实
- **更新顺序**：`_TKWF` 先变（源码/文档/版本发布）→ 本仓库后同步。禁止先改本仓库再等 `_TKWF` 追认
- **版本核对**：本仓库任何版本号（README"当前同步版本"、功能点描述）必须与 `_TKWF/docs/CHANGELOG.md` 及源码一致；不一致时以 `_TKWF` 为准修正本仓库
- **引用方式**：本仓库文章需引用源文档时，引用 `_TKWF/docs/` 下的 D/G/T 文档标题与编号（如 D07A、G06），不得引用本仓库自身的转述作为依据
- **内容冲突时**：以 `_TKWF` 源码 + 源文档为准，冲突方为本仓库，须修正

### 2.1 构建与发布规则

- 本文档站基于 DocFX（`docs/docfx.json`），只构建 `articles/**`、`api/**`、`toc.yml`、`index.md`
- **`docs/` 根目录下的新 `.md` 文件不会出现在网页上，但会进公开 git 仓库**——内部内容勿放此处
- 文档站内容修改（articles、toc、首页）提交到本 public 仓库前需确认无敏感信息
- 部署：推送 `main` 触发 GitHub Actions 自动构建到 GitHub Pages

### 2.2 版本同步机制（prebuild 脚本）

> 维护 3 处版本号同步点，避免手动更新遗漏。

**Pre-build 脚本**：`docs/prebuild.ps1`

- 在 `docfx build` 前执行，自动从 `_TKWF/docs/CHANGELOG.md` 提取最新 3 版本
- 同步 3 处维护点：
  1. `docs/llms.txt` — 当前同步版本号
  2. `docs/index.md` — 版本动态区块（最近 3 版本表格）
  3. `docs/articles/agentic/source-doc-map.md` — 对齐 TKWF 版本号
- 本地执行：`pwsh docs/prebuild.ps1`
- CI 集成：GitHub Actions workflow 中在 `docfx build` 前添加 `- run: pwsh docs/prebuild.ps1`

**打 tag 一句话**：`pwsh scripts/tag.ps1`（自动取 TKWF 最新 v* tag → 同步版本 → 提交 → tag → 推送）

### 2.3 内容对齐流程（TKWF 新版本发布时）

> 对齐 tag = 同步 tag = 对齐内容 = 对齐框架的最新文档。

| 步骤 | 动作 | 说明 |
|:-----|:------|:------|
| 1 | **更新文档内容** | 将文档站内容对齐 TKWF 最新版本（新增特性、修正 API 名、更新示例代码） |
| 2 | **审核 + 记录** | 创建 `docs/变更记录/V4.9.x-内容对齐记录.md`，记录本次对齐的变更内容 |
| 3 | **提交 + 推送 + tag** | `pwsh scripts/tag.ps1`（自动同步版本号、提交、打 tag 推送） |

**内容变更记录模板**（位置：`docs/变更记录/`）：

```markdown
# TKWF.Docs 内容对齐记录 — V4.9.x

> 对齐 TKWF：V4.9.x · 日期：YYYY-MM-DD
> 前置版本：V4.9.y（日期）

## 本次对齐

| 类型 | 说明 |
|:-----|:------|
| 新增 | 新版本新增特性对应的文档更新 |
| 修改 | API 变更、术语修正、示例代码更新 |
| 删除 | 废弃功能对应的文档移除 |

## 涉及文件

| 文件路径 | 变更类型 |
|:---------|:---------|
| `docs/articles/...` | 新增/修改/删除 |

## 审核

| 审核人/工具 | 结果 | 日期 | 意见摘要 |
|:-----------|:-----|:-----|:---------|
| … | ✅ 通过 | YYYY-MM-DD | … |
```

### 2.4 版本号维护清单（每次打 tag 前检查）

- [ ] 运行 `pwsh docs/prebuild.ps1` 同步 3 处版本号
- [ ] 检查 `README.md` 的"当前同步版本"是否与 `_TKWF` CHANGELOG 一致
- [ ] 检查 `docs/articles/` 下文章中的版本引用是否过时
- [ ] 检查 `docs/404.md` / `docs/robots.txt` 中的版本/链接引用
- [ ] 构建验证：`docfx build docs/docfx.json` 零 warning

---

## 三、通用工作规则

- 文档改动前先读 `../_TKWF/docs/00-文档体系说明.md`，遵循 D/G/T 分类
- 术语变更（如 `UseNoAop<T>()` → `Use<T>()`）需跨仓库 grep 清理
- 提交前检查 git status，只 add 本次相关的文件

---

## 四、文档同步规则（_TKWF/docs → TKWF.Docs）

> **适用场景**：`_TKWF/docs/` 发生文档变更（新增/修改/重命名/归档/重编号），需要将变更同步到 TKWF.Docs 站点。
> **与 §二.3 的区别**：§二.3 "内容对齐流程"针对 TKWF 框架版本发布（tag 级别），本节针对**源文档体系自身的结构性变更**——即使框架版本未升级，文档体系也可能重编号/新增/归档。

### 4.1 同步触发条件

收到 "_TKWF/docs 有文档更新" 或 "同步文档" 指令时，按以下步骤判定同步范围：

| 步骤 | 命令 | 说明 |
|:-----|:------|:------|
| 1 | `git -C ../_TKWF log --since='<上次同步时间>' --name-status --format='--- %h %s' -- docs/` | 列出上次同步以来 `_TKWF/docs/` 的所有文件变更 |
| 2 | 读 `../_TKWF/docs/00-文档体系说明.md` | 获取最新文档体系全貌（D/G/T/AC 系列编号、关键词、修订记录），这是**结构变化的权威来源** |
| 3 | `git -C ../_TKWF tag --list 'v*' --sort=-v:refname | Select-Object -First 3` | 确认最新 TKWF 版本号 |
| 4 | `git log --oneline -5` | 确认本仓库上次同步点 |

### 4.2 变更类型与同步动作矩阵

| `_TKWF/docs/` 变更类型 | 标识（git status） | TKWF.Docs 同步动作 | 涉及文件 |
|:-----------------------|:-------------------|:-------------------|:---------|
| **新增源文档**（D/G/T/AC 系列） | `A` | source-doc-map 新增行；如涉及新主题分类则新增分节 | `source-doc-map.md` |
| **源文档重命名/重编号** | `R` | source-doc-map 更新编号映射；grep 全仓库旧编号引用并替换 | `source-doc-map.md` + articles 中引用旧编号的文件 |
| **源文档归档**（移入 `_Archive/`） | `R`（移路径） | source-doc-map 删除对应行或标注"已归档" | `source-doc-map.md` |
| **源文档内容修改** | `M` | 检查 articles 是否有衍生内容需同步；source-doc-map 描述列按需更新 | `source-doc-map.md` + 相关 articles |
| **新增文档系列**（如 AC 系列） | `A` 多个 | source-doc-map 新增分节；设计方案 §3.2 新文章清单引用的源编号更新 | `source-doc-map.md` + 设计方案 |
| **文档体系整理**（00-文档体系说明.md 修改） | `M` | 全量对比 source-doc-map 与 00-文档体系说明，补齐缺失条目、更新过期描述 | `source-doc-map.md` |

### 4.3 source-doc-map.md 更新规则

source-doc-map.md 是 TKWF.Docs 与 `_TKWF/docs/` 的**唯一映射桥梁**，更新时遵循：

1. **分区对齐**：source-doc-map 的分节应与 `00-文档体系说明.md` 的 D/G/T/AC 分类对应，不自行发明分类
2. **编号一致性**：源文档编号（如 D07B、G07C）必须与 `00-文档体系说明.md` 表格中的编号**逐字一致**——重编号后旧编号不保留
3. **描述精简**：每个条目描述列控制在 1-2 句，概括核心内容 + 版本/状态标注（如 "V1.0.5"、"v1.2.2"、"已归档"）
4. **公开文章列**：有对应公开文章的填链接（`../path/to/article.md`），无对应的填 `—`
5. **版本标记行**：末尾 `> 对齐 TKWF：V{x.y.z}` 由 prebuild.ps1 自动同步，不手动修改
6. **重编号处理**：源文档重编号后，source-doc-map 直接使用新编号，**不在映射表中保留旧编号→新编号的映射**（旧编号通过 git 历史和变更记录追溯）

### 4.4 articles 内容同步规则

当源文档**内容修改**时，需判断公开文章是否需要同步：

| 判定条件 | 动作 |
|:---------|:-----|
| 公开文章引用了被修改源文档的具体内容（API 名、代码示例、架构描述） | 同步更新公开文章 |
| 公开文章只引用源文档的编号/标题作为"进一步阅读"链接 | 无需同步 |
| 源文档重编号，公开文章引用了旧编号 | grep 全仓库替换旧编号 → 新编号 |
| 源文档归档，公开文章引用了该文档 | 更新引用为替代文档或删除引用 |

**grep 旧编号引用**（重编号场景必做）：

```powershell
# 示例：G07F → G07C 重编号后，搜索全仓库旧编号引用
grep -r "G07F" docs/articles/      # 找到引用
grep -r "G07F" docs/变更记录/      # 变更记录是历史记录，不修改
```

> **注意**：`docs/变更记录/` 目录下的历史记录文件**不修改**——它们是当时事实的记录，保留旧编号。

### 4.5 同步验证清单

同步完成后，逐项检查：

- [ ] `source-doc-map.md` 条目数量与 `00-文档体系说明.md` 的 D/G/T/AC 表格一致
- [ ] source-doc-map 中无已归档文档的残留条目（或已标注"已归档"）
- [ ] source-doc-map 中无旧编号残留（全仓库 grep 确认）
- [ ] `README.md` "当前同步版本"与 `_TKWF` 最新 tag 一致
- [ ] `pwsh docs/prebuild.ps1` 执行通过（同步 llms.txt/index.md/source-doc-map 版本行）
- [ ] `docfx build docs/docfx.json` 零 warning（如安装了 docfx）
- [ ] 创建内容对齐记录（`docs/变更记录/`）

### 4.6 同步操作模板

```powershell
# 1. 确认 _TKWF 最新版本
git -C ../_TKWF tag --list 'v*' --sort=-v:refname | Select-Object -First 1

# 2. 查看源文档变更
git -C ../_TKWF log --since='<上次同步日期>' --name-status --format='--- %h %s' -- docs/

# 3. 读最新文档体系说明
# （读 ../_TKWF/docs/00-文档体系说明.md）

# 4. 更新 source-doc-map.md
# （编辑 docs/articles/agentic/source-doc-map.md）

# 5. grep 旧编号引用（重编号场景）
grep -r "旧编号" docs/articles/

# 6. 更新 README.md 当前同步版本
# （编辑 README.md）

# 7. 同步版本号
pwsh docs/prebuild.ps1

# 8. 创建内容对齐记录
# （新建 docs/变更记录/V4.9.x-内容对齐记录.md）

# 9. 构建验证（可选，如安装了 docfx）
docfx build docs/docfx.json
```

---

## 五、设计方案更新触发规则

> **适用场景**：判定 `_TKWF/docs/TKWF.Docs_设计文档/TKW.Framework-文档站设计方案-V{x.y.z}.md` 何时需要更新、升版本。
> **设计方案 vs 文档同步的区别**：设计方案是**站点自身的架构蓝图**（导航结构、文章清单、引用模式、首页设计），文档同步是**日常内容对齐**。设计方案变更频率远低于文档同步。

### 5.1 更新触发条件

以下情况**必须**更新设计方案：

| 触发条件 | 升版本类型 | 说明 |
|:---------|:-----------|:-----|
| 新增/删除顶层导航分类（toc.yml 结构性变更） | 子版本 | 如新增"🤖 Agentic 专题"分类 |
| 新增/删除文章（§3.2 新文章清单变化） | 子版本 | 设计方案的新文章清单是站点内容蓝图 |
| 引用模式变更（源文档引用方式、链接策略调整） | 子版本 | 如从"复制内容"改为"交叉链接" |
| 首页设计变更（Hero 文案、对比表、场景卡片调整） | 子版本 | 首页是设计方案的核心产出 |
| 新增文档系列（如 AC 系列出现）且需在站点引用 | 子版本 | 设计方案需记录新系列的存在和引用策略 |
| 源文档**大规模重编号**（如 G07 全系列重编号） | 修订 | 更新设计方案中引用的旧编号 |
| 源文档新增内容需在站点文章中体现 | 修订 | 更新引用源文档列表 |
| 错别字、格式修正、术语同步 | 修订 | 直接修改 |

### 5.2 不需要更新设计方案的情况

| 情况 | 原因 |
|:-----|:-----|
| 源文档内容修改（无编号变化） | source-doc-map 同步即可，设计方案引用的是编号不是内容 |
| 源文档归档（不影响站点导航） | source-doc-map 标注即可 |
| TKWF 框架版本升级（无文档结构变化） | prebuild.ps1 + 内容对齐记录即可 |
| 新增源文档但无对应公开文章 | source-doc-map 新增行即可 |

### 5.3 设计方案与文档同步的协作关系

```
_TKWF/docs 发生变更
        │
        ├── 文档结构变化（新增/重编号/归档/新系列）
        │       │
        │       ├── 文档同步（§四）→ source-doc-map.md 更新
        │       │
        │       └── 是否影响站点导航/文章清单/引用策略？
        │               │
        │               ├── 是 → 更新设计方案（§五）→ 升版本 + 变更记录
        │               └── 否 → 仅文档同步，设计方案不动
        │
        └── 纯内容修改（无结构变化）
                └── 文档同步（§四）→ source-doc-map.md 按需更新
```

### 5.4 设计方案更新检查清单

每次文档同步后，检查是否需要触发设计方案更新：

- [ ] 本次同步是否新增了文档**系列**（如新增 AC 系列）→ 设计方案 §3.2 需记录
- [ ] 本次同步是否涉及源文档**大规模重编号**（如 G07 全系列）→ 设计方案引用的旧编号需更新
- [ ] 本次同步是否需要新增**公开文章**→ 设计方案 §3.2 新文章清单需补充
- [ ] 本次同步是否涉及**导航结构**调整 → 设计方案 §3.1 需更新
- [ ] 本次同步是否涉及**首页**内容变更 → 设计方案 §3.5 需更新
- [ ] 以上全否 → 仅文档同步，设计方案不动

### 5.5 设计方案版本判定流程

```
文档同步完成 → 检查 §5.4 清单
    │
    ├── 全否 → 设计方案不动，本次同步完成
    │
    ├── 有"修订"级变更（旧编号更新、引用列表更新）
    │       → V1.x.y → V1.x.y+1（修订），编写变更记录
    │
    └── 有"子版本"级变更（新增文章/导航/首页/新系列引用）
            → V1.x → V1.x+1（子版本），git mv 更新文件名，编写变更记录
```
