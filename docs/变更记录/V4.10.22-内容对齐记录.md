# TKWF.Docs 内容对齐记录 — V4.10.22

> 对齐 TKWF：V4.10.22 · 日期：2026-09-14
> 前置版本：V4.10.4（2026-09-11）

## 本次对齐

| 类型 | 说明 |
|:-----|:------|
| 同步 | 版本号 V4.10.4 → V4.10.22：llms.txt、index.md（Hero 徽章 + 版本动态表 v4.10.20-22）、source-doc-map 对齐行、content/site.json、content/home/versions.json、content/home/hero.json（prebuild.ps1 自动，git tag 兜底生效） |
| 修改 | README.md 当前同步版本 V4.10.4 → V4.10.22 + 版本动态表补 v4.10.20-22 三行 |
| 修改 | source-doc-map.md：头部文档体系 v4.29 → v4.30；D07 行 `IsGraphQLQueryable` → `ExposeGraphqlQuery`（V4.9.102 统一）；D18A 描述更新（DI001/DI002/DI003 三件套 + 69 条）；D071 描述补 v4.30 状态同步 |
| 修改 | extensions/index.md：Permissions V0.8.0 → +V0.9.0（批量检查）；AuditLogging V0.3.0 → V0.4.0（管理 API）；Notifications V0.2.0 → V0.4.0（偏好路由 + SignalR）；新增 Notifications.SignalR V0.1.0 行；顶部计数 24 → 25 |
| 修改 | gates.md：编译期 DI 依赖验证章节更新——诊断码 `TKWF_DI001` → `TKWF_SG1a_DI001`、升级方式 MSBuild 属性 → `[TKWFSeverity]` 属性、补充 DI001/DI002/DI003 三件套 + 信号驱动化（V4.10.22 ADR74/75）+ `[DiContractIgnore]` 豁免（ADR71）；版本演进表补 V4.10.8-22 |
| 修改 | background-jobs.md：租户上下文恢复补 V4.10.15 ambient 自动捕获说明 |

## 涉及文件

| 文件路径 | 变更类型 |
|:---------|:---------|
| `docs/llms.txt` | 修改（prebuild.ps1 自动同步 V4.10.22） |
| `docs/index.md` | 修改（Hero 徽章 + 版本动态表 v4.10.20-22） |
| `docs/articles/agentic/source-doc-map.md` | 修改（头部 v4.30 + D07/D18A/D071 描述 + 对齐行 V4.10.22） |
| `docs/content/site.json` | 修改（prebuild.ps1 自动） |
| `docs/content/home/versions.json` | 修改（prebuild.ps1 自动，3 条） |
| `docs/content/home/hero.json` | 修改（prebuild.ps1 自动） |
| `README.md` | 修改（当前同步版本 + 版本动态表补 v4.10.20-22 行） |
| `docs/articles/explanation/extensions/index.md` | 修改（扩展版本同步 + SignalR 新增） |
| `docs/articles/explanation/gates.md` | 修改（DI 校验章节 + 版本演进） |
| `docs/articles/explanation/background-jobs.md` | 修改（ambient 租户捕获补充） |
| `docs/变更记录/V4.10.22-内容对齐记录.md` | 新增（本文件） |

## 变更背景

`_TKWF` 自 v4.10.4（2026-09-11）以来发布 **v4.10.22**（2026-09-14），期间 18 个版本（v4.10.5-v4.10.22），文档体系升级 v4.29 → v4.30：

- **编译期 DI 校验体系闭环（v4.10.8-22）**：DI001（构造依赖校验）扫描面收敛演进——internal 排除（ADR68）→ `[DiContractIgnore]` 豁免（ADR71）→ 信号驱动化（ADR74）；DI002 跨扩展补全（ADR65，Store/DataService 注入面）；DI003 扩展聚合遗漏（ADR75）；`[TKWFSeverity]` 属性统一升级开关（弃用 MSBuild 属性）
- **方法级 Expose 门控（v4.10.20-21）**：`[ApiExpose(ExposeGraphQL=false)]` 方法级生效（ADR70）+ 方法级 ExposeRest 完成（ADR72）
- **Tier 1.5 测试基础设施（v4.10.13-16）**：SQLite 内存库多 Fact 隔离、DateTime UTC 往返、重置契约修正
- **后台作业 ambient 租户捕获（v4.10.15）** + G13 路径段约定补全
- **BUG004 扩展生成物内部化（v4.10.17）**：CS0436 根治（ADR67 归属边界）
- **文档体系 v4.30**（2026-09-11）：D071 §二场景 2 解决状态同步（V4.10.2 迁移落地），G07 §4 SQL 列裁剪状态 ⚠️→✅
- **扩展仓库**：Permissions v0.9.0（`IPermissionBatchChecker` 批量检查）、AuditLogging v0.4.0（管理 API）、Notifications v0.4.0（偏好路由 + 逐用户权限门控）+ Notifications.SignalR v0.1.0（独立包）；各扩展 MinVer 独立版本化（`{扩展名}/v` 前缀 tag）

## 遗留事项

- `_TKWF` CHANGELOG 格式漂移（v4.10.x 无独立版本头）持续存在；prebuild.ps1 git tag 兜底已覆盖，CI 侧 `_TKWF` checkout 已加 `fetch-tags: true`（上轮修复）
- v4.10.5-19 中间版本的详细变更未逐条写入本站版本动态表（仅 v4.10.20-22 补表）；如需回溯可查 `_TKWF` CHANGELOG `[Unreleased]` 块
