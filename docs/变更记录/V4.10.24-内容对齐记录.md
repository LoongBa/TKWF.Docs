# TKWF.Docs 内容对齐记录 — V4.10.24

> 对齐 TKWF：V4.10.24 · 日期：2026-09-15
> 前置版本：V4.10.22（2026-09-14）

## 本次对齐

| 类型 | 说明 |
|:-----|:------|
| 同步 | 版本号 V4.10.22 → V4.10.24：llms.txt、index.md（Hero 徽章 + 版本动态表 v4.10.22-24）、source-doc-map 对齐行（含日期）、content/site.json、content/home/versions.json、content/home/hero.json（prebuild.ps1 自动，git tag 兜底生效） |
| 修改 | README.md 当前同步版本 V4.10.22 → V4.10.24 + 版本动态表补 v4.10.23/v4.10.24 两行 |
| 无 | source-doc-map 文档体系仍 v4.30（v4.10.23-24 无文档体系结构变化）；站点文章无受影响内容 |

## 涉及文件

| 文件路径 | 变更类型 |
|:---------|:---------|
| `docs/llms.txt` | 修改（prebuild.ps1 自动同步 V4.10.24） |
| `docs/index.md` | 修改（Hero 徽章 + 版本动态表 v4.10.22-24） |
| `docs/articles/agentic/source-doc-map.md` | 修改（对齐行 V4.10.24 + 日期） |
| `docs/content/site.json` | 修改（prebuild.ps1 自动） |
| `docs/content/home/versions.json` | 修改（prebuild.ps1 自动，3 条） |
| `docs/content/home/hero.json` | 修改（prebuild.ps1 自动） |
| `README.md` | 修改（当前同步版本 + 版本动态表补 v4.10.23/24 行） |
| `docs/变更记录/V4.10.24-内容对齐记录.md` | 新增（本文件） |

## 变更背景

`_TKWF` 自 v4.10.22（2026-09-14）以来发布 **v4.10.24**（2026-09-15），期间 2 个版本：

- **v4.10.23**：xCodeGen 生成期可插拔校验阶段（ADR76 U3b 落地）——`IValidationChecker` 接口（Fast/Heavy 两档）+ `ValidationRunner` + `StateStore`，`CodeGenConfig` 加 `Validation` 节（默认 `Enabled=false`，既有消费端零行为变化）；快速档 Checker ×2（`SqlTypeNameCompatibilityChecker` + `CrossEntityBaseTableColumnChecker`）；SG 单一职责收窄（保留结构级校验 VIEW001/002/003，零新增诊断）；ADR55 D3 废弃
- **v4.10.24**：xCodeGen 活态文档生成缺陷修复 G9/G10——DOMAIN_MAP.md 收录 VEntity（`Model.Entities.Concat(Model.Views)`）+ 实体描述去占位（`ExtractHandWrittenSummary` 优先手写 `/// <summary>`，模板治本移除生成文件占位 `<summary>`）；测试稳定性修复（WorkerTests 轮询同步放宽 + ResolverNameConflictTests 行尾兼容）

**文档体系**：仍 v4.30（2026-09-11，D071 场景 2 状态同步）——v4.10.23-24 无 D/G 编号变化、无 source-doc-map 条目增减、无站点文章受影响内容。

## 遗留事项

- `_TKWF` CHANGELOG 格式漂移（v4.10.x 无独立版本头）持续存在；prebuild.ps1 git tag 兜底已覆盖，不受影响
