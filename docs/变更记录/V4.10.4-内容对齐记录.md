# TKWF.Docs 内容对齐记录 — V4.10.4

> 对齐 TKWF：V4.10.4 · 日期：2026-09-11
> 前置版本：V4.9.92（2026-09-04）

## 本次对齐

| 类型 | 说明 |
|:-----|:------|
| 修复 | **prebuild.ps1 版本解析失效修复**：`_TKWF` CHANGELOG 自 v4.9.93 起格式漂移（v4.10.x 全部堆积在 `[Unreleased]` 块无独立版本头、v4.9.93+ 版本头无日期），日期正则解析不到导致本站版本号停滞在 V4.9.92。新增 **git tag 权威版本兜底**（`git tag --list 'v*' --sort=-v:refname` 取最新 3 版本；描述从 CHANGELOG 匹配 `（V4.10.x）` 标注 bullet 或 git log 兜底） |
| 同步 | 版本号 V4.9.92 → V4.10.4：llms.txt、index.md（Hero 徽章 + 版本动态表 v4.10.2-4.10.4）、source-doc-map 对齐行、content/site.json、content/home/versions.json、content/home/hero.json（prebuild.ps1 自动） |
| 修改 | README.md 当前同步版本 V4.9.92 → V4.10.4 + 版本动态表补 v4.10.4/v4.10.3/v4.10.2 三行 |
| 修改 | source-doc-map.md：头部文档体系 v4.27 → v4.29（D 系列新增 D071）；G07 行改名 `G07-查询执行路径总览.md` → `G07-查询管线总览.md`（v2.0，2026-09-11）+ 描述更新；数据与查询分节新增 D071 条目（SQL 级投影与列裁剪支持矩阵，v4.28/29 新增） |
| 修改 | query-guide.md：源文档参考表 G07 标题"查询执行路径总览" → "查询管线总览" |
| 修改 | extensions/index.md：扩展一览表全量更新（V4.9.92 时代 9 扩展 → 当前 24 扩展 + 契约包，版本对齐扩展仓库 README v0.8.2 时代状态） |
| 修改 | usage.md：现有扩展清单全量更新（P0 9/11 → P0 全部实施 + 24 扩展现状），指向扩展仓库 README 为权威源 |
| 修改 | cqrs-read-write.md：§5 "AutoQuery 自动查询" 章节重写为 "标准查询（EQR + GraphQL Connection）"（VEntity 仅 GraphQL 查询、无 REST 端点、`AutoQuery`/`IsGraphQLQueryable` 旧属性移除）；生成物表/关系表/适用场景同步 |
| 修改 | ventity-aggregate.md：§② "AutoQuery 自动查询" 章节重写为 "标准查询（EQR + GraphQL Connection）"；顶层描述/三层能力表/完整流程/版本演进表同步 |
| 修改 | background-jobs.md：Provider 章节从"规划"更新为三实现落地（内置 v4.9.103 + Hangfire v4.9.104 + Quartz v4.9.105）；JobRecord 持久化/状态查询/重试/分布式锁/优雅停机；现状边界同步 |
| 修改 | gates.md：门控缺口 #8（ProjectMetaContext 初始化）状态 ⏳ Phase 2 → ✅ V4.9.102；版本演进表补 V4.9.102 |

## 涉及文件

| 文件路径 | 变更类型 |
|:---------|:---------|
| `docs/prebuild.ps1` | 修改（git tag 权威版本兜底，修复 CHANGELOG 格式漂移导致的版本停滞） |
| `docs/llms.txt` | 修改（prebuild.ps1 自动同步 V4.10.4） |
| `docs/index.md` | 修改（Hero 徽章 + 版本动态表 v4.10.2-4.10.4） |
| `docs/articles/agentic/source-doc-map.md` | 修改（头部 v4.29 + G07 改名 + D071 新增行 + 对齐行 V4.10.4） |
| `docs/content/site.json` | 修改（prebuild.ps1 自动） |
| `docs/content/home/versions.json` | 修改（prebuild.ps1 自动，3 条） |
| `docs/content/home/hero.json` | 修改（prebuild.ps1 自动） |
| `README.md` | 修改（当前同步版本 + 版本动态表补 v4.10.2-4.10.4 行） |
| `docs/articles/advanced/query-guide.md` | 修改（G07 标题引用更新） |
| `docs/articles/explanation/extensions/index.md` | 修改（扩展一览表全量同步） |
| `docs/articles/explanation/extensions/usage.md` | 修改（现有扩展清单同步） |
| `docs/articles/explanation/cqrs-read-write.md` | 修改（§5 标准查询重写 + AutoQuery 相关表述） |
| `docs/articles/explanation/ventity-aggregate.md` | 修改（§② 标准查询重写 + 相关表述） |
| `docs/articles/explanation/background-jobs.md` | 修改（三实现 Provider + JobRecord 持久化落地） |
| `docs/articles/explanation/gates.md` | 修改（门控缺口 #8 收尾状态 + 版本演进） |
| `docs/变更记录/V4.10.4-内容对齐记录.md` | 新增（本文件） |

## 变更背景

`_TKWF` 自 v4.9.92（2026-09-03）以来发布 **v4.10.4**（2026-09-11），期间版本链 v4.9.93 → v4.10.4（v4.9.93-99 未打 tag、v4.10.0-4.10.4 已打 tag），同时文档体系升级 v4.27 → v4.29：

- **v4.10.0（ADR32 xCodeGen 工具链中枢）**：SG2 元数据稳定路径（`_XCG_CopyApiMetadata` → 解决方案根 `.TKWF/{域}/ApiMetadata.g.cs`）+ 脚手架防漂移门禁（双层哈希校验）+ 双份脚手架全量同步
- **v4.10.1（ADR32 决策 5-7 + B 组优化）**：AC-Kit 可执行资源归档至 `_Archive/AC-Kit-可执行资源归档-v4.10.0/`、二进制单点（TemplatesPath 回退链）、命令面统一（决策 7 第二步降级为可选）+ TKWFDomain 显式对齐
- **v4.10.2**：DataService 纯实体 IQueryable 路径迁移方案 1（GraphQL SQL 级列裁剪对齐实体连接路径）
- **v4.10.3**：跨命名空间同名 Controller resolver hintName 相撞修复
- **v4.10.4**：SG 嵌套类型兼容修复（Roslyn `+` 分隔误判）+ Oracle P1-1 CS1737 吸收
- **文档体系 v4.28**（2026-09-11）：G07 改名"查询管线总览"（v2.0）——新增 §0 定位与权威源、§4 管线分层与 ORM 接入矩阵、§5 VEntity 查询管线；新增 `02-迭代开发/SQL级投影与列裁剪支持矩阵.md`（活文档）+ G17A/G17B 描述修正
- **文档体系 v4.29**（2026-09-11）：SQL 投影矩阵编号化（**D071**）+ 精化注 A/B + Q3 遗留迭代登记；G07 §0/§7.2 权威源表改指 D071
- **扩展仓库 `_TKWF.Extensions`**：扩展从 v0.1.0 时代演进到 24 扩展 + 契约包（v0.8.2 时代），新增 Approval/Calendar/FileManagement/FeatureManagement/Dashboard/DataPort/PrintTemplates/RateLimiting/SecurityLog/OrganizationUnit/Notifications/BackgroundJobs/HealthCheck 等，既有扩展版本大幅升级（Tagging v0.4.0、Identity/Account v0.3.0 等）

## 文章调整（第二批，2026-09-11）

根据源文档变化同步的站点文章衍生内容（§4.4 规则）：

- **VEntity 查询语义变更（v4.9.102）**：VEntity 明确不支持 REST 端点（SG1b early-return 阻断）、`AutoQuery`/`IsGraphQLQueryable` 旧属性移除、统一 `ExposeRestQuery`/`ExposeGraphqlQuery`——cqrs-read-write.md/ventity-aggregate.md 的"AutoQuery 自动查询"章节重写为"标准查询（EQR + GraphQL Connection）"，标注 REST 需 Service 包装
- **后台任务三实现落地（v4.9.103-105）**：内置轻量调度（JobRecord 持久化 + worker + 重试/分布式锁/优雅停机）+ Hangfire Provider + Quartz Provider——background-jobs.md 从"规划中"更新为三实现现状（含 `IAdvancedBackgroundJobManager` 状态查询/取消）
- **门控缺口 #6/#8 收尾（v4.9.102）**：`EventDispatchFilter` 规则 + `ProjectMetaContextInitialized` 规则——gates.md 缺口 #8 状态更新

## 遗留事项

- 中间版本 v4.9.93-99（未打 tag）的详细变更未逐条写入本站版本动态表（仅 v4.10.2-4.10.4 补表）；如需回溯可查 `_TKWF` CHANGELOG `[Unreleased]` 块
- `_TKWF` CHANGELOG 格式漂移（v4.10.x 无独立版本头）建议上游择机整理为 `## [x.y.z] — date` 标准格式；prebuild.ps1 已通过 git tag 兜底不受影响
