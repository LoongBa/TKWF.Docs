# TKWF.Docs 内容对齐记录 — V4.9.26

> 对齐 TKWF：V4.9.26 · 日期：2026-08-12
> 前置版本：V4.9.22（2026-08-10）

## 本次对齐

| 类型 | 说明 |
|:-----|:------|
| 修改 | 同步版本号 V4.9.22 → V4.9.26：llms.txt、index.md Hero 徽章、README.md 当前同步版本 |
| 修改 | index.md 版本动态区更新为最新 3 版本（4.9.26 / 4.9.24 / 4.9.23，CHANGELOG 中 v4.9.25 内容合并于 v4.9.26 条目） |
| 修改 | README.md 版本动态区更新为 4.9.26 / 4.9.25 / 4.9.24 / 4.9.23 |
| 修改 | source-doc-map.md 对齐版本 → V4.9.26；D02 条目反映拆分（D02A-D02D 客户端认证分册） |
| 修改 | `_TKWF` 打 v4.9.26 tag |

## 涉及文件

| 文件路径 | 变更类型 |
|:---------|:---------|
| `docs/llms.txt` | 修改（prebuild 同步） |
| `docs/index.md` | 修改（Hero 徽章 + prebuild 版本动态同步） |
| `README.md` | 修改（当前同步版本 + 版本动态区） |
| `docs/articles/agentic/source-doc-map.md` | 修改（对齐版本 + D02 拆分映射） |
| `docs/变更记录/V4.9.26-内容对齐记录.md` | 新增（本文件） |

## 对齐背景

- `_TKWF` 自 v4.9.22 后发布 v4.9.23（cfg 强契约加固）、v4.9.24（认证重构 + 四轮加固）、v4.9.25（认证扩展点重构，内容并入 v4.9.26 CHANGELOG 条目）、v4.9.26（限流器适配 System.Threading.RateLimiting）
- D02 文档拆分为 D02A-D02D 客户端认证分册（bdc2146），source-doc-map 映射同步
- 文档站文章内容（认证/配置/宿主集成）不涉及上述框架 API 变更描述，本次仅同步版本号与文档映射

## 审核

| 审核人/工具 | 结果 | 日期 | 意见摘要 |
|:-----------|:-----|:-----|:---------|
| Sisyphus（实施验证） | ✅ 通过 | 2026-08-12 | prebuild 3 处同步成功，tag v4.9.26 |

## 遗留事项

- G07F 指南（tkwf-tsclient V1.0.4 门面工厂）在 `_TKWF` 工作区有未提交修改，待其提交后按需同步 source-doc-map 说明

---

## 追加对齐（2026-08-12）

| 类型 | 说明 |
|:-----|:------|
| 修改 | source-doc-map.md：G07F 条目标注 V1.0.4（Tkwf 门面工厂入口、移除 Call()），新增 G07M 条目（ts-client-mock v1.9.0） |

### 对齐背景

- `_TKWF` G07F 使用指南更新至 V1.0.4（b0e2c11：Tkwf 门面工厂入口 + Call() 移除）
- `_TKWF` 新增 G07M 指南（`@tkwf/tsclient-mock` v1.9.0，Transport 注入 mock 运行时）及配套 skill（tkwf-tsclient-mock / tkwf-tsclient-mock-server），当前为未跟踪新文件

---

## 追加对齐（2026-08-14）

| 类型 | 说明 |
|:-----|:------|
| 修改 | source-doc-map.md：G07F V1.0.4→V1.0.5（transport 注入点）；G07M v1.9.0→v1.2.2（版本号回退）；D07M v2.0.1→v1.2.2（版本号回退） |
| 修改 | 同步版本号 V4.9.26 → V4.9.27（llms.txt、index.md、source-doc-map.md） |

### 对齐背景

- **TKW.Framework** v4.9.27（CHANGELOG）— Phase A-D EntityQueryRoot 回归 + 客户端命名统一（UseApi→Use / QueryApi→Query）；Phase C 明确 `UseApiClient()`/`UseApiService()` 不改，仅 `DomainClientUser.UseApi<T>()`→`Use<T>()`。TKWF.Docs 中无命中需改
- **tkwf-tsclient** v1.0.5（tag v1.0.5）— `DomainHostClientOptions` 新增 `transport` 注入点，支持自定义 Transport（mock/测试用）
- **TKWF-tsclient-mock** v1.2.2（tag v1.2.2，版本号回退背景）— v2.x 开发方案合并回 v1.2.2 发布，v1.3.0–v1.9.0 标记为历史开发 tag（未独立发布）

### 涉及文件

| 文件路径 | 变更类型 |
|:---------|:---------|
| `docs/llms.txt` | 修改（prebuild 同步 V4.9.27） |
| `docs/index.md` | 修改（prebuild 同步 V4.9.27） |
| `docs/articles/agentic/source-doc-map.md` | 修改（对齐版本 V4.9.27 + 3 条目版本号修正） |
| `docs/变更记录/V4.9.26-内容对齐记录.md` | 修改（本段追加） |