# TKWF.Docs 内容对齐记录 — V4.9.43

> 对齐 TKWF：V4.9.43 · 日期：2026-08-21
> 前置版本：V4.9.40（2026-08-18，commit 06b2f8e）

## 本次对齐

| 类型 | 说明 |
|:-----|:------|
| 同步 | 版本号 V4.9.40 → V4.9.43：llms.txt、index.md（Hero 徽章 + 版本动态区）、source-doc-map 对齐行（prebuild.ps1 自动） |
| 修改 | README.md 当前同步版本 V4.9.41 → V4.9.43 + 版本动态表更新（4.9.43/4.9.42/4.9.41/4.9.40） |
| 前提 | `_TKWF/docs/CHANGELOG.md` 最新条目 v4.9.43（2026-08-20）；v4.9.44 tag 存在但无 CHANGELOG 条目（ADR09 Mock 基础设施 feature commit，未正式记录），按 AGENTS.md 以 CHANGELOG 为准 |

## 涉及文件

| 文件路径 | 变更类型 |
|:---------|:---------|
| `docs/llms.txt` | 修改（prebuild.ps1 自动同步 V4.9.43） |
| `docs/index.md` | 修改（prebuild.ps1 自动同步 Hero 徽章 + 版本动态区） |
| `docs/articles/agentic/source-doc-map.md` | 修改（prebuild.ps1 自动同步对齐行 V4.9.43） |
| `README.md` | 修改（当前同步版本 + 版本动态表） |
| `docs/变更记录/V4.9.43-内容对齐记录.md` | 新增（本文件） |

## 变更背景

`_TKWF` 自 v4.9.40（2026-08-18）以来发布：

### v4.9.41（2026-08-18，fc85971）
- 文档同步：EntityQuery→Query + AdminHardDelete→AdminDelete + DefaultOrder 安全默认（Id 倒序）
- ADR15 Phase 2：删除过期方法 shim + 行为切换（破坏性，5.0.0）
- G07 查询路径总览 v1.1：修正三端能力对比
- ADR16 收窄为 C# ApiClient 专属，TS 端增强拆分至 ADR17

### v4.9.42（2026-08-19，0c72b29，ADR16）
- C# ApiClient QueryBuilder 增强：`CountAsync`（仅计数省带宽）+ `Clone`（并行查询）+ `OrderByField`/`ThenByField`（动态排序，camelCase 归一）

### v4.9.43（2026-08-20，2b96d72）
- 代码质量修复（ADR 外评审发现）：安全（异常消息不泄漏客户端）+ 资源泄漏（HttpRequestMessage using + GraphQLClient IDisposable）+ 可观测性（Debug.WriteLine → ILogger + 空 catch 补日志）+ 死代码删除（UserSessionProvider/IUserSessionProvider）

### 未对齐项：v4.9.44（tag 存在，CHANGELOG 无条目）
- ADR09 Mock 数据基础设施 C# 版（`_Domain.Testing/Mock/`：BogusDataGenerator、MockDbEntityDAC、MockDataSpec、DatasetSeed、FakerMethodMap 等）
- 按 AGENTS.md §二 依赖方向（以 `_TKWF/docs/CHANGELOG.md` 为准），本仓库不自行推断 v4.9.44 为正式版本；待框架组补 CHANGELOG 后再对齐

### ts-client-mock 侧（独立仓库，非本仓库对齐范围）
- v1.4.0-v1.5.0 版本演进：MockDataSpec 规则驱动生成、场景化生成、computed 管线修正、gen-seed CLI、session handlers 补齐（D07M/G07D/AC-Kit 同步）——source-doc-map 中 G07M/D07M 描述无需变更（版本信息已含）

## 审核

| 审核人/工具 | 结果 | 日期 | 意见摘要 |
|:-----------|:-----|:-----|:---------|
| prebuild.ps1 | ✅ 通过 | 2026-08-21 | 3 处版本号同步成功（llms.txt/index.md/source-doc-map）V4.9.43 |
| CHANGELOG 核对 | ✅ 通过 | 2026-08-21 | 最新条目 v4.9.43，v4.9.44 无条目按规则不纳入 |

## 遗留事项

- `_TKWF` v4.9.44（ADR09 Mock 基础设施）待框架组补充 CHANGELOG 条目后对齐
- 设计方案（TKW.Framework-文档站设计方案.md）本轮无需更新——无文章结构/导航/首页变更（§5.2 判定）